//
//  ImageZoomView.swift
//  UCLA Dining
//
//  Created by Rohan Sehgal on 12/26/22.
//

import SwiftUI
import Foundation

//struct ImageZoomView: View {
//    var body: some View {
 //       ScrollView(.horizontal, showsIndicators: false) {
     //       Image("image")
   //             .resizable()
       ///         .aspectRatio(contentMode: .fit)
          //      .frame(maxWidth: .infinity)
            //    .scaleEffect(scale, anchor: anchor)
                //.offset(offset.value)
               // .gesture(
              //      MagnificationGesture()
                   //     .onChanged { value in
                 //           self.scale = value
                     //       self.anchor = .center
                       //     let delta = value - self.lastScale
                         //   let newOffsetX = self.offset.value.width + delta * self.offset.value.width
                          //  let newOffsetY = self.offset.value.height + delta * self.offset.value.height
                           // self.offset.value = CGSize(width: newOffsetX, height: newOffsetY)
                         //   self.lastScale = value
                       // }
                        //.onEnded { _ in
                          //  self.scale = 1.0
                            //self.offset.value = .zero
                            //self.lastScale = 1.0
                        //}
                //)
        //}
    //}

    // Use a scale effect to zoom in and out
    //@State private var scale: CGFloat = 1.0
    //@State private var anchor: UnitPoint = .center
    //@State private var offset: Binding<CGSize> = .constant(.zero)
    //@State private var lastScale: CGFloat = 1.0
//}
